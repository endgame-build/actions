{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Tome PR Metadata",
  "description": "Constrained output emitted by process-tome-comments-ci skill after applying edits to the working tree.",
  "type": "object",
  "additionalProperties": false,
  "required": ["title", "body", "addresses_comment_ids"],
  "properties": {
    "title": {
      "type": "string",
      "minLength": 1,
      "maxLength": 70,
      "description": "One-line PR title (≤70 chars, no trailing period)."
    },
    "body": {
      "type": "string",
      "minLength": 1,
      "description": "Markdown PR body. References comment authors and cluster intent."
    },
    "addresses_comment_ids": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "format": "uuid"
      },
      "description": "Comment IDs this PR resolves. Typically the full cluster."
    }
  }
}
