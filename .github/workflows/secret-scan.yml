# Managed by github-iac.
# DO NOT EDIT OR REMOVE. Edits are detected and reverted.
name: endgame-secret-scan

on:
  workflow_dispatch: {}
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: write

jobs:
  scan:
    uses: endgame-build/github-iac/.github/workflows/secret-scan.yml@main
    secrets: inherit
